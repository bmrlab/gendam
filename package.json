{
  "name": "muse-v2-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
		"web": "pnpm --filter @muse/web --",
    "dev:web": "pnpm web dev",
    "build:web": "pnpm web build",
    "desktop": "pnpm run --filter @muse/desktop --",
		"tauri": "pnpm desktop tauri"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
		"pnpm": ">=8.0.0",
		"npm": "pnpm",
		"yarn": "pnpm",
		"node": ">=20.1 <21"
	}
}
